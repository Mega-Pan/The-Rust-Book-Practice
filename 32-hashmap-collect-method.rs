use std::collection::Hashmap;

fn main(){
    let teams = vec![String::from("Blue"),String::from("Yellow")];
    let initial_score = vec![10,50];

    let mut score: HashMap<_,_> = 
        tems.into_iter().zip(initial_score.into_iter()).collect(); 
}